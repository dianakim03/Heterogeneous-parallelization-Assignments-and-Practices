```mermaid
flowchart TD
    A([Start]) --> B[Задает список размеров N]
    B --> C[Печатает заголовок таблицы]
    C --> D{Берет очередной N}
    D --> E[Создает массивы A B Ccpu Cgpu]
    E --> F[Заполняет A и B случайными значениями]
    F --> G[Запускает CPU сложение и замеряет время]
    G --> H[Проверяет CPU результат]
    H --> I[Ищет GPU устройство OpenCL]
    I --> J{GPU найден}
    J -->|Нет| K[Ставит gpu_ms минус один и ok_gpu FAIL]
    J -->|Да| L[Создает контекст и очередь OpenCL]
    L --> M[Читает kernel_add.cl]
    M --> N[Собирает программу и создает kernel]
    N --> O[Создает буферы dA dB dC]
    O --> P[Копирует A и B на GPU]
    P --> Q[Задает аргументы kernel]
    Q --> R[Запускает kernel и ждет finish]
    R --> S[Читает Cgpu с GPU]
    S --> T[Считывает profiling время]
    T --> U[Проверяет GPU результат]
    U --> V[Освобождает ресурсы OpenCL]
    K --> W[Печатает строку таблицы]
    V --> W
    W --> D
    D -->|Размеры закончились| X([End])
