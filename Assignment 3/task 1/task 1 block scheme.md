```mermaid
flowchart TD
    A([Start]) --> B[Задать N и k]
    B --> C[Создать массив a на CPU]
    C --> D[Заполнить a начальными значениями]
    D --> E[Выделить память на GPU для d_a]
    E --> F[Скопировать a с CPU на GPU]

    F --> G[Замер времени Global kernel]
    G --> H[Запуск kernel_global]
    H --> I[Синхронизация GPU]
    I --> J[Записать время Global]

    J --> K[Скопировать d_a на CPU]
    K --> L[Проверить корректность результата]

    L --> M[Скопировать исходный a снова на GPU]
    M --> N[Замер времени Shared kernel]
    N --> O[Запуск kernel_shared]
    O --> P[Синхронизация GPU]
    P --> Q[Записать время Shared]

    Q --> R[Скопировать d_a на CPU]
    R --> S[Проверить корректность результата]
    S --> T[Освободить память GPU и CPU]
    T --> U([End])
