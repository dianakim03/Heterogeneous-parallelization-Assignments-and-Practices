```mermaid
flowchart TD
    A([Start]) --> B[Задать N и список BLOCKS]
    B --> C[Создать массивы h_a h_b h_c на CPU]
    C --> D[Заполнить h_a и h_b начальными значениями]
    D --> E[Выделить память на GPU для d_a d_b d_c]
    E --> F[Скопировать h_a и h_b с CPU на GPU]

    F --> G{Перебор размеров блока}
    G --> H[Взять текущий blockSize]
    H --> I[Обнулить d_c на GPU]
    I --> J[Запустить kernel_add и замерить время]
    J --> K[Скопировать d_c на CPU в h_c]
    K --> L[Проверить корректность результата]
    L --> M[Вывести blockSize время и статус OK или FAIL]
    M --> G

    G -->|Все размеры блока пройдены| N[Освободить память GPU]
    N --> O[Освободить память CPU]
    O --> P([End])
